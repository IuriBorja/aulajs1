const random = Math.floor(Math.random() * 11);

console.log(random);

const resultRandom = random >= 5 ? 'User VIP' : 'Normal user'; // isso é
// a mesma coisa que escrever o if de baixo 


/*

    if (random >= 5) {
        console.log('User VIP');
    } else {
        console.log('Normal user');
    }
 
 */




console.log(resultRandom);